name: Build & Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Install lftp
        run: sudo apt-get install -y lftp

      - name: Deploy to main domain
        continue-on-error: true
        run: |
          lftp -e "
            set ftp:ssl-allow no;
            set ftp:transfer-mode binary;
            set ftp:passive-mode yes;
            set net:timeout 60;
            set net:max-retries 5;
            set net:reconnect-interval-base 5;
            open -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_SERVER }};
            mirror -R --delete --no-perms ./out/ /public_html/;
            quit
          "

      - name: Deploy to lensscraft.de
        continue-on-error: true
        run: |
          lftp -e "
            set ftp:ssl-allow no;
            set ftp:transfer-mode binary;
            set ftp:passive-mode yes;
            set net:timeout 60;
            set net:max-retries 5;
            set net:reconnect-interval-base 5;
            open -u ${{ secrets.LENSSCRAFT_DE_FTP_USERNAME }},${{ secrets.LENSSCRAFT_DE_FTP_PASSWORD }} ${{ secrets.LENSSCRAFT_DE_FTP_SERVER }};
            mirror -R --delete --no-perms ./out/ /public_html/;
            quit
          "

      - name: Deploy to atlasstv.com
        continue-on-error: true
        run: |
          lftp -e "
            set ftp:ssl-allow no;
            set ftp:transfer-mode binary;
            set ftp:passive-mode yes;
            set net:timeout 60;
            set net:max-retries 5;
            set net:reconnect-interval-base 5;
            open -u ${{ secrets.ATLASSTV_FTP_USERNAME }},${{ secrets.ATLASSTV_FTP_PASSWORD }} ${{ secrets.ATLASSTV_FTP_SERVER }};
            mirror -R --delete --no-perms ./out/ /public_html/;
            quit
          "

      - name: Deploy to lensscraftv.com
        run: |
          lftp -e "
            set ftp:ssl-allow no;
            set ftp:transfer-mode binary;
            set ftp:passive-mode yes;
            set net:timeout 60;
            set net:max-retries 5;
            set net:reconnect-interval-base 5;
            open -u ${{ secrets.LENSSCRAFTV_FTP_USERNAME }},${{ secrets.LENSSCRAFTV_FTP_PASSWORD }} ${{ secrets.LENSSCRAFTV_FTP_SERVER }};
            mirror -R --delete --no-perms ./out/ /public_html/;
            quit
          "
